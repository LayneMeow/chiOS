To build: 'make clean', 'make', './build.sh'
-
Run in Qemu with: './run.sh'.
-
To run on real hardware, flash the image.iso to a usb-drive and boot it on your machine.
-
run the 'HELP' command to see a list of all the shell commands.
-
